# Codex / 专业人员资料

本文件夹保存实现约束与维护信息，不作为普通用户的操作手册。

- [本地资料库技术说明](LOCAL_LIBRARY.md)
- [部署与结构审计](DEPLOYMENT_AUDIT.md)

维护规则：`category` 只能是 `paper` 或 `study_material`；不要在页面组件中直接访问 IndexedDB；不要把用户文件写入代码仓库或上传接口。
