# 时间规划 App 新对话需求汇总

> 给新的 Codex 对话使用。请直接在现有项目上继续开发，不要重新创建项目。

## 1. 项目位置

当前项目文件夹已经从旧名 `时间桂花app` 重命名为：

```bash
/Users/mark/Documents/时间规划app
```

新对话请把工作目录选择为上面的路径。旧路径 `/Users/mark/Documents/时间桂花app` 已不存在。

## 2. 项目技术栈

- React + Vite
- TypeScript
- Tailwind CSS
- lucide-react 图标
- 无后端、无登录、无支付、无云同步
- 所有数据使用 mock data + localStorage
- 移动端优先，目标宽度约 iPhone 390px

运行命令：

```bash
cd /Users/mark/Documents/时间规划app
npm install
npm run dev
npm run build
npm run export:html
```

项目内已有本地 Node/npm 环境，如果系统 npm 不可用，可使用：

```bash
PATH=/Users/mark/Documents/时间规划app/.local-node/node-v24.14.0-darwin-arm64/bin:$PATH npm run dev
```

单文件分享版输出：

```bash
/Users/mark/Documents/时间规划app/share/time-planning-demo.html
```

## 3. 产品名称与定位

产品名：**时间规划**

定位：面向中国国际学生，尤其是 A-Level 学生的学习时间管理软件。

目标用户：

- 中国国际学校学生
- A-Level 学生
- IB / AP / IGCSE 学生
- SAT / IELTS / TOEFL 备考学生

核心方向不是普通日历，而是一个面向国际生学业场景的时间规划系统：

- 学校课表
- 学习日历
- 大考 / 模拟考试
- 作业 DDL
- 科目与单元进度
- 专注番茄钟
- A-Level 搜题工具

## 4. 当前 UI 方向

已经从第一版奶油彩色风格，改为第二版：

- 黑白 / 浅灰为主
- 白色卡片
- 圆角更圆滑
- 少用彩色，保留红色用于大考 / 突发事件
- 最大宽度不要太宽，约 `390px`
- 手机端优先
- 页面信息密度适中
- 整体参考用户发的 StudyPlan 黑白 UI 图，但保留当前 App 原有结构

全局相关文件：

- `src/styles.css`
- `tailwind.config.ts`
- `src/components/BottomNav.tsx`
- `src/App.tsx`

## 5. 当前底部导航

底部 5 个 Tab：

1. 今天
2. 日历
3. 科目
4. 课表
5. 搜题

注意：原来的“专注”不再放底部 Tab，但专注页仍存在，可以从任务、科目复习、单元等入口进入。

## 6. 已实现的主要流程

### 首次进入流程

1. 欢迎页
2. 输入名字
3. 选择年级 / 阶段
4. 选择课程体系
5. 选择考试局
6. 选择科目
7. 进入课表设置页
8. 用户自己设置每门课每周几次、星期、开始时间、结束时间、教室
9. 进入今天首页

相关文件：

- `src/pages/WelcomePage.tsx`
- `src/pages/OnboardingPage.tsx`
- `src/pages/SetupTimetablePage.tsx`
- `src/App.tsx`

### 今天首页

已实现：

- 显示用户姓名
- 大考倒计时翻页卡片
- 今日安排
- 今日任务
- 今日专注时长
- A-Level 搜题入口

文件：

- `src/pages/TodayPage.tsx`

### 日历

已实现：

- 横向日期时间轴
- 进入日历自动定位到今天
- 大考标红
- 临时考试和大考颜色区分
- 学校课表自动显示在日历上
- 点击时间块进入独立编辑页
- 设置放假时间
- 日期详情列表

最近一次修改：

- 每天列宽从 `112px` 缩短到 `78px`
- 左侧时间轴缩到 `38px`
- 每小时高度改成 `42px`
- UI 改成白色圆角卡片
- 全天事件标签更短，减少拥挤

文件：

- `src/pages/CalendarPage.tsx`
- `src/pages/EditTimeBlockPage.tsx`

### 课表

已实现：

- 课程管理 list
- 每门课总次数和总时长
- 每周几次的批量设置
- 单节课微调
- 用户不设置 B 周时默认不添加 B 周
- 可修改每门课的星期、开始时间、结束时间、教室、颜色

文件：

- `src/pages/TimetablePage.tsx`
- `src/pages/SetupTimetablePage.tsx`

### 科目与单元

已实现：

- 科目列表
- 科目详情
- 总进度
- 下次模拟考试时间和大考时间分开
- 单元详情
- 单元分成学习进度和刷题进度
- 进度由用户自己调整

文件：

- `src/pages/SubjectsPage.tsx`
- `src/pages/SubjectDetailPage.tsx`
- `src/pages/UnitDetailPage.tsx`

### 番茄钟

已实现：

- 删除固定 25 / 50 / 15 分钟模式
- 只保留自定义专注时长
- 自定义每几轮休息一次
- 自定义休息分钟
- 完成专注后自动进入休息
- 历史只记录完成时间和时长
- 标题交给用户自定义

文件：

- `src/pages/FocusPage.tsx`

### 搜题页

新增面向 A-Level 学生的搜题页面。

已实现：

- 底部导航“搜题”
- 按用户选择的科目显示本地 mock 题库
- 支持关键词搜索
- 支持按科目筛选
- 显示 Paper、topic、题目、解题思路
- 记录最近搜索到 localStorage

文件：

- `src/pages/QuestionSearchPage.tsx`

## 7. 数据与 localStorage

主要 mock data：

- `src/data/mockData.ts`

主要类型：

- `src/types.ts`

已使用 localStorage 保存：

- 用户 onboarding 状态
- 用户 profile
- 任务
- 课表
- 今日时间轴
- 假期
- 单元进度
- 番茄钟记录
- 当前番茄钟目标
- 番茄钟设置
- 搜题最近搜索

注意：

- 新用户首次进入时不要默认添加“社团活动面试 / 突发事件”
- 旧本地数据里的 `task-club-interview` 和 `line-club` 已在 `App.tsx` 中做启动清理

## 8. 当前关键文件结构

```text
src/
  App.tsx
  main.tsx
  styles.css
  types.ts
  components/
    BottomNav.tsx
    Chip.tsx
    PageHeader.tsx
    ProgressBar.tsx
    StatBubble.tsx
    TaskCard.tsx
  data/
    mockData.ts
  pages/
    AddTaskPage.tsx
    CalendarPage.tsx
    EditTimeBlockPage.tsx
    FocusPage.tsx
    MistakesPage.tsx
    OnboardingPage.tsx
    QuestionSearchPage.tsx
    SetupTimetablePage.tsx
    StudyTasksPage.tsx
    SubjectDetailPage.tsx
    SubjectsPage.tsx
    TimelinePage.tsx
    TimetablePage.tsx
    TodayPage.tsx
    UnitDetailPage.tsx
    WelcomePage.tsx
  utils/
    date.ts
    storage.ts
```

## 9. 后续开发注意事项

- 不要重新搭建项目
- 不要改回旧产品名 `Finished` / `StudyPal`
- 不要使用旧路径 `时间桂花app`
- 修改文件时基于 `/Users/mark/Documents/时间规划app`
- UI 继续保持第二版黑白浅灰、白色圆角、手机端优先
- 尽量减少说教文案，让界面更像真实用户软件
- 日历和课表是核心，不要做成纯视觉 demo
- 保持 `npm run build` 可通过
- 修改后最好同步运行 `npm run export:html`

## 10. 最近一次用户需求

用户最近要求：

- 日历横轴缩短，让手机上一屏能塞入更多日期和信息
- 日历 UI 做白色简化和圆滑处理

已完成：

- `CalendarPage.tsx` 中 `dayColumnWidth = 78`
- `timeColumnWidth = 38`
- `hourHeight = 42`
- 日历主体、假期设置、详情区域已白色圆角化
- 已验证 `npm run build` 和 `npm run export:html`

