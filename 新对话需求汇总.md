# 时间规划 App 新对话需求汇总

> 给新的 Codex 对话使用。请直接在现有项目上继续开发，不要重新创建项目。

## 1. 项目位置

当前项目文件夹：

```bash
/Users/mark/Documents/时间规划app
```

旧路径 `/Users/mark/Documents/时间桂花app` 已不存在。新对话必须基于当前路径继续。

## 2. 项目技术栈

- React + Vite
- TypeScript
- Tailwind CSS
- lucide-react 图标
- 无后端、无登录、无支付、无云同步
- Web MVP 使用 mock data + localStorage
- 移动端优先，同时已开始适配横屏手机、平板横屏和电脑端宽屏布局

建议验证命令：

```bash
npm run build
node scripts/build-preview.mjs
```

项目内已有本地 Node/npm 环境；如果系统 npm 不可用，再使用项目内 `.local-node` 环境。不要把本地运行、外部测试、分享链接或发布部署流程写进 GitHub README。

## 3. 产品名称与定位

产品名：**时间规划**

定位：面向中国国际学生，尤其是 A-Level 学生的学习时间管理软件。

目标用户：

- 中国国际学校学生
- A-Level 学生
- IB / AP / IGCSE 学生
- SAT / IELTS / TOEFL 备考学生

核心方向不是普通日历，而是一个面向国际生学业场景的时间规划系统：

- 学校课表
- 学习日历
- 大考 / 模拟考试
- 作业 DDL
- 科目与单元进度
- 专注番茄钟
- A-Level 搜题工具

## 4. 当前 UI 方向

当前视觉方向：

- 黑白 / 浅灰为主
- 白色卡片
- 圆角更圆滑
- 少用彩色，红色只用于大考 / 突发事件等高优先级状态
- 手机竖屏保留接近 iPhone 390px 的单列体验
- 768px 以上使用桌面侧边栏和多栏布局，最大内容宽度约 `1440px`
- 页面信息密度适中
- 整体参考用户发的 StudyPlan 黑白 UI 图，但保留当前 App 原有结构

全局相关文件：

- `src/styles.css`
- `tailwind.config.ts`
- `src/components/AppLayout.tsx`
- `src/components/BottomNav.tsx`
- `src/App.tsx`

## 5. 当前导航

手机竖屏保留底部 5 个 Tab：

1. 今天
2. 日历
3. 科目
4. 课表
5. 搜题

注意：原来的“专注”不再放底部 Tab，但专注页仍存在，可以从任务、科目复习、单元等入口进入。

横屏 / 桌面端使用左侧侧边栏导航，当前页面要有明显高亮，右侧内容区域独立滚动。

## 6. 已实现的主要流程

### 首次进入流程

1. 欢迎页
2. 输入名字
3. 选择年级 / 阶段
4. 选择课程体系
5. 选择考试局
6. 选择科目
7. 进入课表设置页
8. 用户自己设置每门课每周几次、星期、开始时间、结束时间、教室
9. 进入今天首页

相关文件：

- `src/pages/WelcomePage.tsx`
- `src/pages/OnboardingPage.tsx`
- `src/pages/SetupTimetablePage.tsx`
- `src/App.tsx`

### 今天首页

已实现：

- 显示用户姓名
- 大考倒计时翻页卡片
- 今日安排
- 今日任务
- 今日专注时长
- A-Level 搜题入口
- 桌面端左侧放今日任务、搜题入口和今日日历
- 桌面端右侧放番茄钟和专注时间模块

文件：

- `src/pages/TodayPage.tsx`

### 日历

已实现：

- 横向日期时间轴
- 进入日历自动定位到今天
- 大考标红
- 临时考试和大考颜色区分
- 学校课表自动显示在日历上
- 点击时间块进入独立编辑页
- 设置放假时间
- 日期详情列表
- 电脑端左侧选择月份和日期，右侧查看 / 管理当天时间线
- 课程 / 休息是有开始和结束时间的日程，任务是带 DDL 的事项
- 中国假期、考试、特别事项、DDL 在左侧月历用小标签标注

最近一次 UI 修改：

- 桌面端打招呼模块移动到右侧时间线顶部
- 左侧月历获得更多纵向空间
- 左侧月历新增 `中假/假`、`考`、`特`、`DDL` 图例
- 本月统计显示考试、特别事项、假期

文件：

- `src/pages/CalendarPage.tsx`
- `src/pages/EditTimeBlockPage.tsx`

### 课表

已实现：

- 课程管理 list
- 每门课总次数和总时长
- 每周几次的批量设置
- 单节课微调
- 用户不设置 B 周时默认不添加 B 周
- 可修改每门课的星期、开始时间、结束时间、教室、颜色

文件：

- `src/pages/TimetablePage.tsx`
- `src/pages/SetupTimetablePage.tsx`

### 资料库 / 科目

已实现：

- 科目列表改为资料库入口
- 按科目整理本地文件，帮助用户快速寻找资料
- 显示全部文件、真题、答案、待归档和文件夹数量
- 错题本入口保留在资料库页
- 科目详情
- 下次模拟考试时间和大考时间分开
- 单元详情

文件：

- `src/pages/SubjectsPage.tsx`
- `src/pages/SubjectDetailPage.tsx`
- `src/pages/UnitDetailPage.tsx`

### 番茄钟

已实现：

- 删除固定 25 / 50 / 15 分钟模式
- 只保留自定义专注时长
- 自定义每几轮休息一次
- 自定义休息分钟
- 完成专注后自动进入休息
- 历史只记录完成时间和时长
- 标题交给用户自定义

文件：

- `src/pages/FocusPage.tsx`

### 搜题页

新增面向 A-Level 学生的搜题页面。

已实现：

- 底部导航“搜题”
- 电脑端左侧选择科目、年份、月份、卷号
- 卷号右侧显示 Pure Math / Mechanics / Statistics 类型
- 选中试卷后直接给出试卷和答案入口
- 右侧顶部保留拍照上传搜题入口
- 试卷状态：没做过、做过一遍、正在学习、完整理解
- 未做过默认为白色书签；其他状态用低饱和书签色条提示
- 错题记录：输入错题题号，可选是否记录分数
- 按用户选择的科目显示本地 mock 题库
- 支持关键词搜索
- 支持按科目筛选
- 显示 Paper、topic、题目、解题思路
- 记录最近搜索到 localStorage

文件：

- `src/pages/QuestionSearchPage.tsx`
- `src/features/questionSearch/`

## 7. 数据与 localStorage

主要 mock data：

- `src/data/mockData.ts`

主要类型：

- `src/types.ts`

已使用 localStorage 保存：

- 用户 onboarding 状态
- 用户 profile
- 任务
- 课表
- 今日时间轴
- 假期
- 单元进度
- 番茄钟记录
- 当前番茄钟目标
- 番茄钟设置
- 搜题最近搜索
- 试卷学习状态
- 试卷错题题号与可选分数
- 本地资料库文件统计

注意：

- 新用户首次进入时不要默认添加“社团活动面试 / 突发事件”
- 旧本地数据里的 `task-club-interview` 和 `line-club` 已在 `App.tsx` 中做启动清理

## 8. 当前关键文件结构

```text
README.md
CHANGELOG.md
新对话需求汇总.md
src/
  App.tsx
  main.tsx
  styles.css
  types.ts
  components/
    AppLayout.tsx
    BottomNav.tsx
    Chip.tsx
    PageHeader.tsx
    ProgressBar.tsx
    StatBubble.tsx
    TaskCard.tsx
  data/
    mockData.ts
  features/
    questionSearch/
  pages/
    AddTaskPage.tsx
    CalendarPage.tsx
    EditTimeBlockPage.tsx
    FocusPage.tsx
    MistakesPage.tsx
    OnboardingPage.tsx
    QuestionSearchPage.tsx
    SetupTimetablePage.tsx
    SettingsPage.tsx
    StudyTasksPage.tsx
    SubjectDetailPage.tsx
    SubjectsPage.tsx
    TimelinePage.tsx
    TimetablePage.tsx
    TodayPage.tsx
    UnitDetailPage.tsx
    WelcomePage.tsx
  utils/
    date.ts
    localFiles.ts
    storage.ts
docs/archives/
  2026-07-04-responsive-webapp.md
backend/caie_papers/
content/cie-math/
wechat-miniprogram/
```

## 9. 后续开发注意事项

- 不要重新搭建项目
- 不要改回旧产品名 `Finished` / `StudyPal`
- 不要使用旧路径 `时间桂花app`
- 修改文件时基于 `/Users/mark/Documents/时间规划app`
- UI 继续保持第二版黑白浅灰、白色圆角、手机端优先
- 不要把桌面端做成拉宽的手机页；优先使用侧边栏、两栏 / 三栏和独立滚动
- 尽量减少说教文案，让界面更像真实用户软件
- 日历和课表是核心，不要做成纯视觉 demo
- 保持 `npm run build` 可通过
- 当前阶段不要再生成单文件 HTML；网站更新通过 GitHub 分支发布

## 10. README 内容规则

所有 README 只保留长期有价值的公开项目内容：

- 项目定位
- 网站 / App 需求
- 技术栈与架构
- 已完成功能汇总
- 设计思路
- 模块职责
- 数据边界
- 变更记录规则

不要在 README 中写：

- 发给别人测试的方法
- 临时分享链接或 Netlify Drop 等流程
- 微信小程序发布部署、AppID 修改、平台后台配置
- 只服务一次性协作的本地操作说明

## 11. 变更记忆与存档规则

这是后续 Codex 必须遵守的提示词：

> 记住每一次更改。日常小改不必立即创建新分支；在开发者提示汇总对话、阶段性总结更改或准备交接时，再为当前存档创建新的 change 文档，记录相对上一次存档的所有变化。

执行细则：

- 每次修改都要记住并保留要点，但不要因为每一个小改就创建新分支。
- 阶段性汇总时，为当前存档创建新的 change 文档；只有需要保留阶段快照时，再创建日期或编号存档分支，例如 `archive/2026-07-04-search` 或 `archive/v003`。
- change 文档顶部先写给人看的变更 list；每条 50 字内说清具体改了什么，例如修复 bug、调整 UI 设计、新增搜题功能。
- 变更 list 下方再补充细节，包括涉及页面 / 模块、关键文件、验证结果、未完成事项，以及相对上一次存档的变化。
- 如果当前仓库还没有可比较的上一次存档，要明确写“暂无历史存档”，不要假装已经比较过。
- 最终回复用户时说明改了哪些文档、是否改代码、是否运行验证。

## 12. 最近一次用户需求

用户最近要求：

- 日历电脑端左侧月历要标注中国假期、考试和特别事项。
- 日历页面左侧保留月历预览，右侧放打招呼模块和当天时间线。
- 完成后把七月四号存档写入 GitHub main，并把网站推到线上页面。
- main 存档需要写清已完成部分、设计思路和相对上一次存档的新增内容。

已完成：

- `gh-pages` 已发布网站资源 `time-planning-preview-20260705-1434`。
- `CalendarPage.tsx` 已新增中国假期、考试、特别事项、DDL 小标签和图例。
- `README.md` 已更新响应式布局、日历、资料库、搜题和设计思路。
- `CHANGELOG.md` 已新增 `2026-07-04 · responsive-webapp-archive`。
- 新增 `docs/archives/2026-07-04-responsive-webapp.md` 记录本次相对上一次存档的变化。
